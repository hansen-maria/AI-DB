services:
  # Vue.js frontend (configured with nginx)
  vue-app:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ai-db-frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - certbot-webroot:/var/www/certbot:ro
      - certbot-certs:/etc/letsencrypt:ro
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - app-network

  # Rust/Axum Backend
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ai-db-api
    expose:
      - "8000"
    environment:
      - RUST_LOG=info
      - BAKTA_DB=/bakta-db
      - AI_DB_TEMP_DIR=/tmp-data
      - AI_DB_JOBS_PATH=/data/jobs.db
      - AI_DB_KPI_PATH=/kpi-db/kpi.db
      - AI_DB_CUSTOM_ANNOTATIONS_PATH=/custom-db/custom_annotations.db
      - ADMIN_KPI_SECRET=${ADMIN_KPI_SECRET:-}
    volumes:
      - /mnt/bakta-db/db:/bakta-db:ro
      - /mnt/ai-db-custom:/custom-db
      - /mnt/ai-db-tmp:/tmp-data
      - jobs-data:/data
      - /mnt/ai-db-kpi:/kpi-db
    restart: unless-stopped
    networks:
      - app-network

  # Certbot
  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - certbot-webroot:/var/www/certbot
      - certbot-certs:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    restart: unless-stopped
    networks:
      - app-network

volumes:
  certbot-webroot:
  certbot-certs:
  jobs-data:

networks:
  app-network:
    driver: bridge